// DO NOT EDIT — this file is automatically generated by `npx cap update android`
// or `npx cap sync android` based on the plugins listed in package.json.
// It is committed here as a placeholder so the project tree is complete;
// running `npx cap sync android` will regenerate it with your actual plugin list.

apply plugin: 'com.android.application'

android {
    compileSdkVersion project.hasProperty('compileSdkVersion') ? rootProject.ext.compileSdkVersion : 34
    defaultConfig {
        minSdkVersion project.hasProperty('minSdkVersion') ? rootProject.ext.minSdkVersion : 22
        targetSdkVersion project.hasProperty('targetSdkVersion') ? rootProject.ext.targetSdkVersion : 34
    }
}

dependencies {
    // Capacitor core + installed plugins are wired in automatically by `cap sync`, e.g.:
    // implementation project(':capacitor-push-notifications')
    // implementation project(':capacitor-camera')
    // implementation project(':capacitor-geolocation')
}

if (hasProperty('postBuildExtras')) {
    postBuildExtras()
}
